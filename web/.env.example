# Internal URL used only by the React Router server/BFF.
API_BASE_URL=http://127.0.0.1:8788

# Required in production. Generate at least 32 random bytes and keep it secret.
SESSION_SECRET=replace-with-at-least-32-random-characters
