import { FeaturePlaceholder } from "~/features/placeholders/feature-placeholder";

export default function Dashboard() {
  return (
    <FeaturePlaceholder
      contract="The first implementation will combine server aggregations with deterministic temporal-core projections. Analytical queries stay separate from live activity delivery."
      eyebrow="Desktop workspace"
      summary="Compare temporal distributions, rarity, recurrence, tags, actors, and source activity without compressing the data into a mobile feed."
      title="Temporal analytics"
    />
  );
}
