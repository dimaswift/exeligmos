import { FeaturePlaceholder } from "~/features/placeholders/feature-placeholder";

export default function Records() {
  return (
    <FeaturePlaceholder
      eyebrow="Data management"
      summary="Search, filter, create, edit, reference, and inspect public or encrypted records through the generated API contract."
      title="Records"
    />
  );
}
