import { FeaturePlaceholder } from "~/features/placeholders/feature-placeholder";

export default function Events() {
  return (
    <FeaturePlaceholder
      eyebrow="Temporal intervals"
      summary="Inspect lightweight events across ranges, actors, devices, numeric types, and metadata."
      title="Events"
    />
  );
}
