/**
 * Feed-specific presentation controls. Keeping this outside the glyph engine
 * lets the site change visual density without changing canonical Saros data.
 */
export const activityPresentationConfig = Object.freeze({
  glyphDepth: 5,
});
